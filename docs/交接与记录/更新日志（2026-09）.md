# 更新日志（2026-09）

## 2026-09-05：个人路线 BE-01

- 对照孟鑫杰个人路线，补齐 `Conversation.version` 类型修复：Java/MyBatis 已统一为 `Integer`/`INTEGER`。
- 新增 `20260905_be01_conversation_version_up.sql`、`down.sql`、`verify.sql`；正向迁移会先列出并阻断非整数或超出 Java `Integer` 范围的历史值。
- 更新 `lx.sql` 基线定义并新增 Mapper 映射测试。
- 本次只完成代码、基线和迁移脚本；未执行云 MySQL 迁移，未修改 BE-10～14 业务实现。

## 2026-09-05：个人路线 BE-02

- 登录成功后使用 `user-token:<userId>` 保存当前 Token 索引，二次登录先删除旧的 `token:<token>`，再写入新 Token；Token TTL 保持 30 分钟。
- 退出登录同步清理用户 Token 索引，避免索引残留。
- 新增 `TokenControllerTest`，验证旧 Token 删除及新 Token、用户索引写入。
- `mvn -o clean test`：120 项通过、0 失败、1 项跳过；未执行云端部署验证，未推送。

## 2026-09-11：BE-14-2 限流原子性与配置收口

- `@RateLimit` 未显式指定上限时读取 `AI_PRIVACY_RATE_LIMIT_PER_MINUTE`，默认每用户每路径每分钟 60 次。
- Redis 限流计数改为 Lua 原子脚本，在同一 Redis 操作中完成 `INCR` 与首次 `EXPIRE`，避免并发下窗口未设置或重复设置。
- 超限响应补充 `Retry-After`（秒）响应头，JSON 结构保持 `status/msg/requestId/data`。
- 更新限流单元测试，验证 Lua 执行、key 和窗口参数；全量测试 191 项通过、0 失败、1 项按既有约定跳过。

## 2026-09-12：BE-15-1 个性化反馈闭环

- 新增迁移 `sql/migrations/20260912_be15_ai_feedback_up/down/verify.sql`，建 `ai_feedback` 表（每任务唯一，JSON 存薄弱点/推荐练习，保留源 payload）。
- 新增 `AiFeedback` 实体、`AiFeedbackMapper`+XML、`AiFeedbackService`+实现、`AiFeedbackView` DTO。
- 生成策略：优先消费任务最新 `validation_finished` 事件（`conclusion/checks/suggestions`）；缺失时按本地规则基于真实回答、引用来源和提问内容生成，来源标记 `RULE_FALLBACK`。
- 新增接口 `GET /api/ai/tasks/{taskId}/feedback`，校验任务所有权（跨用户 404），读取时惰性生成并落库，保证幂等。
- `AiRetentionService` 保留期清理、按会话/任务删除同步覆盖 `ai_feedback`，避免隐私删除残留。
- 新增 `AiFeedbackServiceImplTest`（5 项）与 `AiTaskControllerTests` 反馈委托用例；`mvn -o clean test` 197 项通过、0 失败、1 项跳过。
- 新增 `docs/交付材料/报告与PPT/隐私保护说明.md`（BE-14-4），如实描述脱敏工具与当前接入边界。

## 2026-09-12：BE-14-1 脱敏接入日志/错误/导出

- 新增 `util/LogbackPrivacyConverter` 与 `resources/logback-spring.xml`，通过 `%maskedMsg` 对全部日志正文中的手机号/身份证/邮箱脱敏（日志后端为 Logback，log4j-api 经 `log4j-to-slf4j` 桥接）。
- `AiMessageServiceImpl.saveAssistantError` 错误信息落库前脱敏。
- `AiModelCallLogController` 模型调用日志 CSV/JSON 导出前对文本字段脱敏。
- 新增 `LogbackPrivacyConverterTest`（3 项）与 `AiMessageServiceImplTest` 脱敏用例；`mvn -o clean test` 201 项通过、0 失败、1 跳过。
- 更新 `隐私保护说明.md` 第 7/8/10 节，如实反映已接入范围与剩余边界。

## 2026-09-12：数据库迁移与种子数据验证（P2）

- 在云 MySQL 8.4.7 上新建临时库 `lx_verify_be15` 做全量验证（不影响生产 `lx`）：`lx.sql`、`lx_add.sql` 及全部 AI 迁移（be08/be11/be10gaps/be12/be14/be01_version/be01_conversation/be15）正向执行全部通过，对应 verify 脚本全部 exit 0；验证完成后删除临时库。
- `sql/seed/demo_ai_data.sql` 连续执行两次均 exit 0，计数幂等：`ai_task` 6、`ai_subtask` 6、`ai_event` 63、`ai_message` 6、`ai_evidence` 12、`ai_model_call_log` 18、`ai_conversation` 3；满足"任务≥5、事件≥50、证据≥10、日志≥12"验收阈值。
- 修复种子脚本缺陷：`demo_ai_data.sql` 曾硬编码 `USE lingxi;`，与库名 `lx` 不符且云库无 `lingxi`，直接报 `Unknown database`；改为由调用方指定目标库。
- 修复 BE-11 迁移缺陷：`20260824_be11_ai_conversation_up.sql` 的回填 SELECT 在 `only_full_group_by` 下报 `ERROR 1055`，在 MySQL 8 必然失败；将非聚合标题表达式包入 `MAX()`。
- `README.md` 导入命令补充 `--default-character-set=utf8mb4`，避免中文数据导入报 `ERROR 1366`。
- 生产 `lx` 实况核对：`ai_task/ai_subtask/ai_event/ai_message/ai_evidence/ai_model_call_log/ai_conversation/ai_config/ai_audit_log` 已存在，`ai_message` 含 `attachments/error_code/error_message`，`lx_conversation.version` 已是 `int` 且表为空；当时唯一缺失的 `ai_feedback` 已执行 be15 `up` 并 verify 通过（11 列就绪）。

