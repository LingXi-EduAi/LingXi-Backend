# QA-01 — 后端集成测试扩展（≥45 用例，AI 模块行覆盖率 ≥70%）

- **板块**：B9 测试与压测（Part B 决赛）
- **波次**：B3
- **优先级**：Part B
- **前置依赖**：A0、A3–A6、A9
- **工作量**：2.0 人天

## 目标

后端集成测试覆盖 SSE/权限/恢复/超时/幂等/重试并发/持久化。

## 输入

- A0 测试基建 + Part A/B 全部功能

## 实施内容

1. 扩展 JUnit 套件 ≥45 用例：SSE 流、权限（`@Login`/`@TeacherOnly`）、断线恢复、超时、幂等插入、重试并发、持久化。
2. AI 模块行覆盖率 ≥70%（jaccoco 报告验证）。

## 验收标准

- `mvn test` 绿，用例数 ≥45。
- AI 模块行覆盖率 ≥70%。

## 工作区域

- `LingXi-Backend/src/test/java/**`（扩展）

## 约束

- Java 8。
- 不删除失败测试来"通过"。
