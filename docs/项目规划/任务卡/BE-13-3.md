# BE-13-3 — 模型调用查询 API（含聚合）

- **板块**：A4 模型调用日志（Part A 初赛）
- **波次**：A1
- **优先级**：MUST
- **前置依赖**：BE-13-2、AUTH-01-1
- **工作量**：0.5 人天

## 目标

教师/比赛侧查询模型日志（按用户/时间/应用；平均延迟、token 总量、费用）。

## 输入

- BE-13-2 的 `AiModelCallLogService`
- AUTH-01-1 鉴权基线

## 实施内容

1. 新增 `controller/AiModelCallLogController.java`：
   - `GET /api/ai/model-calls`：按用户/时间/任务过滤 + 聚合统计（平均延迟、token 总量、费用）。
2. 扩展 `AiModelCallLogService` 支持过滤与聚合。
3. 编写 JUnit 契约测试。

## 验收标准

- 查询与聚合可用。
- 教师可见（经 AUTH-01-1 基线角色校验）。
- JUnit 测试绿。

## 工作区域

- 新增 `controller/AiModelCallLogController.java`，扩展 `AiModelCallLogService`，测试

## 约束

- `@Login` 用户隔离强制。
- Java 8。
