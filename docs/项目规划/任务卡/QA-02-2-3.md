# QA-02-2/3 — 前端组件/store 测试 + Playwright E2E

- **板块**：B9 测试与压测（Part B 决赛）
- **波次**：B3
- **优先级**：Part B
- **前置依赖**：A0、A3–A6、A9
- **工作量**：2.0 人天

## 目标

前端单元/组件测试 + E2E 全闭环（登录→创建→流式→停止→刷新→历史仍在）。

## 输入

- A0 测试基建（vitest）+ Part A/B 前端功能

## 实施内容

1. `tests/unit/**`：组件（`AnswerPanel`/`AiComposer`/`SubtaskPanel`/`EvidenceDrawer`/`FeedbackPanel`/侧栏）+ store（`ai` 模块）测试。
2. 新增 `tests/e2e/**` + `playwright.config.js`：Playwright E2E 全闭环场景。

## 验收标准

- `npm run test:unit` 绿；Playwright E2E 全闭环通过。

## 工作区域

- `LingXi-Frontend/tests/unit/**`、`tests/e2e/**`、`playwright.config.js`（新增）

## 约束

- 纯 JS。
- E2E 依赖部署环境可用（可用 DEMO-01-2 种子数据）。
