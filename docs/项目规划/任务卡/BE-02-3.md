# BE-02-3 — 学科路由 `AiAgentRouter.route(query)` 完整版

- **板块**：B3 学科路由（Part B 决赛）
- **波次**：B0（Agent 就绪时）
- **优先级**：Part B
- **前置依赖**：B1（BE-12 动态应用 id）、B11/DIFY-04（学科 Agent 矩阵，外部）、Dify 侧 Agent 就绪（外部）
- **工作量**：1.0 人天

## 目标

不同学科请求路由到对应 Dify 应用，无匹配安全回退 CHATFLOW。

## 输入

- 用户查询文本 + Dify 侧学科 Agent（**外部依赖，需先在 Dify 配置**）+ B1 动态应用 id
- `AiAgentRouter` 既有骨架

## 实施内容

1. 补全 `AiAgentRouter.route(query)` 骨架：关键词规则返回学科应用；回退与无匹配处理。
2. 新增 `AiSubjectRouteRule`。
3. 契约测试。学科规则与 B11（DIFY-04 学科全科化）对齐。

## 验收标准

- `route()` 关键词规则可用；无匹配安全回退。
- 测试绿（学科 Agent 未就绪时 Part A 演示单 Agent Chatflow）。

## 工作区域

- `AiAgentRouter`、新增 `AiSubjectRouteRule`、测试

## 约束

- 不改网关公开 API。
- Java 8。
