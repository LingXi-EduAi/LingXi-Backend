# FE-03-1 — Vuex `ai` 模块（归一化状态）

- **板块**：A5 AI 工作台（Part A 初赛，前端主线）
- **波次**：A2（主线第二步）
- **优先级**：MUST
- **前置依赖**：FE-04-1
- **工作量**：1.5 人天

## 目标

任务/会话/消息/事件/连接状态的唯一真源。

## 输入

- FE-04-1 的 SSE 客户端
- [LingXiEvent协议-v1.md](../../接口与协议/LingXiEvent协议-v1.md)、[AI任务接口契约-v1.md](../../接口与协议/AI任务接口契约-v1.md)

## 实施内容

1. 新增 `store/modules/ai.js`：归一化 state + mutations + actions 覆盖 `createTask`、`streamTask`、`fetchTask`、`stopTask`、`retrySubtask`、`fetchConversations`、`fetchMessages`、`renameConversation`、`deleteConversation`、`continueConversation`。
2. 在 `store/index.js` 注册模块（**不删既有 UI 状态**）。
3. 新增 `tests/unit/aiStore.spec.js`：按会话隔离、序列去重。

## 验收标准

- store 单元测试绿（按会话隔离、序列去重）。
- 模块注册成功，既有状态不受影响。

## 工作区域

- 新增 `src/store/modules/ai.js`、`tests/unit/aiStore.spec.js`
- 修改 `src/store/index.js`（注册模块）

## 约束

- 纯 JS，无 TS。
- 不删既有 UI 状态。
