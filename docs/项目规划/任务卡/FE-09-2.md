# FE-09-2 — 对话交互打磨

- **板块**：B6 对话交互打磨（Part B 决赛）
- **波次**：B1
- **优先级**：Part B
- **前置依赖**：A5
- **工作量**：1.0 人天

## 目标

安全 Markdown 渲染、上传、停止/重生成、错误 toast、流式批渲染。

## 输入

- A5 的基础 `AnswerPanel`/`AiComposer`；`answer_delta` 流；错误场景

## 实施内容

1. 升级 `AnswerPanel.vue`/`AiComposer.vue`。
2. Markdown 白名单清洗（`safeMarkdown.js`）。
3. `answer_delta` 逐帧批渲染（防抖动/合并渲染）。
4. 停止/重生成、错误 toast。

## 验收标准

- 交互打磨完成；测试绿。

## 工作区域

- `AnswerPanel.vue`、`AiComposer.vue`、`safeMarkdown.js`

## 约束

- 纯 JS。
