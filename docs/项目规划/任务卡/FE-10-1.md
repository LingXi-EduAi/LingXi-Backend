# FE-10-1 — 会话历史侧边栏（列表/消息/重命名/删除/继续）

- **板块**：A6 会话管理（Part A 初赛）
- **波次**：A3
- **优先级**：SHOULD
- **前置依赖**：BE-11-1、FE-03-1、FE-05-1
- **工作量**：1.5 人天

## 目标

"刷新不丢状态" — 经 BE-11 的持久会话。

## 输入

- BE-11-1/BE-11-2 API
- FE-03-1 store、FE-05-1 工作台

## 实施内容

1. 新增 `src/components/ai/ConversationSidebar.vue`：列 `GET /api/ai/conversations`，加载 `.../{id}/messages`，PATCH/DELETE 重命名/删除，"继续"把 `conversationId` 传给下一次流式；分页加载更多。
2. store action 接线。
3. 集成进 `AiAssistantPage.vue`。

## 验收标准

- 会话列表/消息/重命名/删除/继续可用。
- 刷新后历史仍在。
- 测试绿。

## 工作区域

- 新增 `src/components/ai/ConversationSidebar.vue` + store action + 集成进 `AiAssistantPage.vue`

## 约束

- 纯 JS。
