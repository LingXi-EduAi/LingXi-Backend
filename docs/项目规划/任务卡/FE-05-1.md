# FE-05-1 — AI 任务工作台页（`AiAssistantPage.vue`）+ 路由 + 守卫

- **板块**：A5 AI 工作台（Part A 初赛，演示核心）
- **波次**：A2（主线第三步）
- **优先级**：MUST — 演示核心
- **前置依赖**：FE-03-1、FE-04-1、AUTH-01-1
- **工作量**：1.5 人天

## 目标

演示主屏：会话导航（后续）、输入框、流式答案、总体状态、停止/重试。

## 输入

- FE-03-1 store、FE-04-1 SSE 客户端
- [AI任务接口契约-v1.md](../../接口与协议/AI任务接口契约-v1.md)（`POST /api/ai/tasks`、`/stop`、`/subtasks/{id}/retry`）

## 实施内容

1. 新增 `src/views/student/AiAssistantPage.vue`：提交 → `POST /api/ai/tasks` → 订阅事件 → 经 `answer_delta` 增量渲染流式答案；停止按钮调 `/stop`；错误渲染安全消息。
2. 新增最小 `src/components/ai/AnswerPanel.vue`、`src/components/ai/AiComposer.vue`。
3. 修改 `src/router/index.js`：加 `/student/ai-workbench` 路由（懒加载，与兄弟路由一致），在（新接线的）角色守卫后。

## 验收标准

- `/student/ai-workbench` 可渲染。
- 提交→流式渲染→停止→重试全流程可用。
- 页面中无任何 Dify key/URL。

## 工作区域

- 新增 `src/views/student/AiAssistantPage.vue`、`src/components/ai/AnswerPanel.vue`（最小）、`src/components/ai/AiComposer.vue`（最小）
- 修改 `src/router/index.js`

## 约束

- 页面中禁止出现 Dify key/URL（走 `baseRequest`）。
- 纯 JS。
