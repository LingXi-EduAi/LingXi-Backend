# FE-11-1 — 教师分析页 `AiAnalytics.vue` + `TaskReplay.vue`

- **板块**：B7 教师分析页（Part B 决赛亮点）
- **波次**：B2
- **优先级**：Part B（决赛亮点）
- **前置依赖**：A4、A5、A6
- **工作量**：2.0 人天

## 目标

`/teacher/ai-analytics`：任务回放（轨迹 + token + 延迟）+ 按 Agent 调用统计图表（完成定义 #7）。

## 输入

- A4 模型日志聚合数据（`/api/ai/model-calls`）+ 任务快照

## 实施内容

1. 新增 `src/views/teacher/AiAnalytics.vue`：任务列表 → 轨迹回放、图表（token/延迟按应用、失败节点）。
2. 新增 `src/components/teacher/TaskReplay.vue`。
3. 添加路由（懒加载、教师角色守卫）。
4. 复用 `FeedbackPanel.vue`（B12）展示反思反馈。

## 验收标准

- 教师可查看轨迹回放与统计；数据来自真实日志。
- 路由角色守卫生效。

## 工作区域

- 新增 `src/views/teacher/AiAnalytics.vue`、`src/components/teacher/TaskReplay.vue` + 路由

## 约束

- 纯 JS。
- 角色守卫（AUTH-01-1 基线）。
