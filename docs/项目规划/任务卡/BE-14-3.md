# BE-14-3 — 审计日志 + 保留策略 + 用户数据删除

- **板块**：B2 隐私与安全加固（Part B 决赛，大创隐私）
- **波次**：B2
- **优先级**：Part B
- **前置依赖**：BE-14-1、BE-14-2
- **工作量**：1 人天

## 目标

who-did-what 审计轨迹 + 数据保留/擦除（大创隐私要求）。

## 输入

- BE-14-1 脱敏基础、BE-14-2 限流
- `logback-spring.xml`

## 实施内容

1. 新增 `service/AiAuditService.java`：MDC requestId/userId，JSON 审计日志覆盖 AI 动作。
2. 保留策略可配。
3. 新增 `controller/AiPrivacyController.java`：`DELETE /api/ai/user-data` 擦除用户 ai_* + 会话行（必要时软删）。
4. 更新 `logback-spring.xml`。
5. 测试。

## 验收标准

- 审计日志覆盖 AI 动作（脱敏）。
- 保留策略可配。
- 用户数据擦除可用。
- 测试绿。

## 工作区域

- 新增 `service/AiAuditService.java`、`controller/AiPrivacyController.java`、更新 `logback-spring.xml`，测试

## 约束

- 审计内容脱敏（BE-14-1）。
- Java 8。
