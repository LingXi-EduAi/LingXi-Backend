# FE-09-1 — 退役直连 Dify 的 `TeachingAssistant.vue`；路由指向工作台

- **板块**：A7 退役直连与残留清理（Part A 初赛）
- **波次**：A4
- **优先级**：MUST（消除最后一个 VUE_APP_AI_KEY 使用者）
- **前置依赖**：FE-05-1
- **工作量**：0.5 人天

## 目标

浏览器代码不再持有/使用 Dify key。

## 输入

- `src/views/student/TeachingAssistant.vue`（最后一个 `VUE_APP_AI_KEY` 使用者）
- `src/router/index.js`

## 实施内容

1. `/student/teaching-assistant` 解析到工作台（alias/redirect 到 `/student/ai-workbench`）。
2. 删除 `TeachingAssistant.vue`。
3. `.env`/`.env.example` 移除 `VUE_APP_AI_KEY`。

## 验收标准

- grep 显示零处读取 `VUE_APP_AI_KEY`。
- 旧路由不 404。

## 工作区域

- `src/router/index.js`、删除 `src/views/student/TeachingAssistant.vue`、编辑 `.env` + `.env.example`

## 约束

- 不把值提交到 `.env`（仅移除）。
