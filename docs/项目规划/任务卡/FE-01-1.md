# FE-01-1 — 删除 `aiRequest` + 残留 Dify API 配置

- **板块**：A7 退役直连与残留清理（Part A 初赛）
- **波次**：A4
- **优先级**：SHOULD
- **前置依赖**：FE-09-1、FE-01-2
- **工作量**：0.5 人天

## 目标

完成 FE-01 清理（后端兼容层 `ApiController` 留到 CLEAN-01 再动）。

## 输入

- `src/utils/api.js`（`aiRequest`）
- `.env.example`

## 实施内容

1. 从 `src/utils/api.js` 删除 `aiRequest`（确认零调用点）。
2. `VUE_APP_AI_API` 仅在 iframe 仍指向 Dify 期间保留（Part B FE-02 移除）。
3. 构建验证。

## 验收标准

- grep 零处 `aiRequest`。
- 构建绿。

## 工作区域

- `src/utils/api.js`、`.env.example`

## 约束

- 不删 `baseRequest`。
- 若 `VUE_APP_AI_API` 仍有 iframe 使用则保留。
