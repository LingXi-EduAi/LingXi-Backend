# FE-06-1 — 认知轨迹时间线组件

- **板块**：A5 AI 工作台（Part A 初赛，演示亮点）
- **波次**：A2→A4（fixture 先，集成后）
- **优先级**：MUST（演示亮点）
- **前置依赖**：FE-03-1、FE-05-1（集成）
- **工作量**：1.5 人天

## 目标

在时间轴上可视化 LingXiEvent 序列（`task_started → task_decomposed → agent_assigned → node_progress → answer_delta → task_finished`）。

## 输入

- FE-03-1 归一化 `eventsByTask`
- [LingXiEvent协议-v1.md](../../接口与协议/LingXiEvent协议-v1.md)

## 实施内容

1. 新增 `src/components/ai/CognitiveTraceTimeline.vue`：消费 `eventsByTask`，按 `sequence` 渲染有序节点，节点详情可展开。
2. 用 fixture 独立运行验证（不必等真实数据）。
3. 集成进 `AiAssistantPage.vue`。

## 验收标准

- 轨迹按 `sequence` 有序渲染。
- 节点详情可展开。
- fixture 独立验证通过。

## 工作区域

- 新增 `src/components/ai/CognitiveTraceTimeline.vue` + 集成进 `AiAssistantPage.vue`

## 约束

- 纯 JS、样式与 Argon 风格一致。
