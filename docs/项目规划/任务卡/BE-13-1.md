# BE-13-1 — AiModelCallLog 实体 + Mapper + XML（表已建）

- **板块**：A4 模型调用日志（Part A 初赛）
- **波次**：A1
- **优先级**：MUST（大创硬性要求 + 视频证据）
- **前置依赖**：QA-00-1
- **工作量**：0.5 人天

## 目标

按任务持久化模型调用记录（token/耗时/费用/错误）。

## 输入

- `ai_model_call_log` 表（BE-08 已建，DDL 见 `sql/migrations/20260809_be08_ai_tables_up.sql`）

## 实施内容

1. 新增 `pojo/AiModelCallLog.java`：字段映射 DDL 全列。
2. 新增 `mapper/AiModelCallLogMapper.java`：按 `task_id` 插入/查询，**幂等插入**（无唯一冲突）。
3. 新增 `resources/mybatis/AiModelCallLogMapper.xml`。
4. 新增测试 `AiModelCallLogMapperTest.java`。

## 验收标准

- 按 `task_id` 插入/查询可用。
- 幂等插入无冲突。
- JUnit 测试绿。

## 工作区域

- 新增 `pojo/AiModelCallLog.java`、`mapper/AiModelCallLogMapper.java`、`resources/mybatis/AiModelCallLogMapper.xml`、测试

## 约束

- 不改 DDL。
- Java 8、风格与既有 Mapper 一致。
