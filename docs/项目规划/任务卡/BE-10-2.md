# BE-10-2 — AiEvidence 实体 + Mapper + XML（表已建）

- **板块**：A3 消息与证据持久化（Part A 初赛）
- **波次**：A1
- **优先级**：MUST
- **前置依赖**：QA-00-1
- **工作量**：0.5 人天

## 目标

持久化每个回答的知识引用。

## 输入

- `ai_evidence` 表（BE-08 已建，DDL 见 `sql/migrations/20260809_be08_ai_tables_up.sql`）
- 既有 Mapper 风格参考

## 实施内容

1. 新增 `pojo/AiEvidence.java`：列 `source_type/title/url/content_snippet/score`。
2. 新增 `mapper/AiEvidenceMapper.java`：按 `message_id` 插入/查询。
3. 新增 `resources/mybatis/AiEvidenceMapper.xml`。
4. 新增测试 `AiEvidenceMapperTest.java`。

## 验收标准

- 按 `message_id` 插入/查询可用。
- 列映射完整。
- JUnit 测试绿。

## 工作区域

- 新增 `pojo/AiEvidence.java`、`mapper/AiEvidenceMapper.java`、`resources/mybatis/AiEvidenceMapper.xml`、测试

## 约束

- 不改 `ai_evidence` DDL。
- Java 8、Lombok、风格与既有 Mapper 一致。
