# BE-10-1 — AiMessage 实体 + Mapper + XML（表已建）

- **板块**：A3 消息与证据持久化（Part A 初赛）
- **波次**：A1
- **优先级**：MUST
- **前置依赖**：QA-00-1
- **工作量**：1 人天

## 目标

持久化每个会话的用户提问与最终回答。

## 输入

- `ai_message` 表（BE-08 已建，DDL 见 `sql/migrations/20260809_be08_ai_tables_up.sql`）
- 既有 Mapper 风格参考：`AiEventMapper` / `AiTaskMapper`

## 实施内容

1. 新增 `pojo/AiMessage.java`：字段映射 `ai_message` DDL 全部列（含 `dify_message_id`）。
2. 新增 `mapper/AiMessageMapper.java`：插入、按 `(conversation_id, created_at)` 分页查询。
3. 新增 `resources/mybatis/AiMessageMapper.xml`：SQL 映射，处理 `dify_message_id` 唯一键冲突。
4. 新增 `src/test/java/.../AiMessageMapperTest.java`：JUnit 5 测试。

## 验收标准

- `AiMessageMapper` 插入/查询可用。
- 按 `(conversation_id, created_at)` 分页查询。
- 字段映射 DDL 全列（含 `dify_message_id` 唯一键处理）。
- JUnit 测试绿。

## 工作区域

- 新增 `pojo/AiMessage.java`、`mapper/AiMessageMapper.java`、`resources/mybatis/AiMessageMapper.xml`、`src/test/java/.../AiMessageMapperTest.java`

## 约束

- 不改 `ai_message` DDL。
- 不动既有 AiTask/AiSubtask/AiEvent。
- Java 8、Lombok、FastJSON、MyBatis XML 风格与既有代码一致。
