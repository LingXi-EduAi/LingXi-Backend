# FE-04-1 — SSE 客户端 + 解析器（`src/services/aiSseClient.js`）+ 测试

- **板块**：A5 AI 工作台（Part A 初赛，前端主线）
- **波次**：A2（主线第一步）
- **优先级**：MUST
- **前置依赖**：QA-02-1
- **工作量**：1 人天

## 目标

可复用、可测试的 SSE 消费者，用于 `POST /api/ai/task/stream`（fetch + ReadableStream + TextDecoder，因 EventSource 仅支持 GET）。

## 输入

- [LingXiEvent协议-v1.md](../../接口与协议/LingXiEvent协议-v1.md)（冻结契约）
- `src/views/student/TeachingAssistant.vue` 的 reader 循环（仅参考抽取，**此处不改该文件**）

## 实施内容

1. 新增 `src/services/aiSseClient.js`：处理分帧（半包、CRLF、多行 `data:`、`\n\n` 分隔）、跳过 `:heartbeat` 注释、解析 `id:`/`event:`/`data:`、产出 `LingXiEvent` 对象、支持 abort/超时、转发 `Last-Event-ID`。
2. （可选）新增纯解析器 `src/utils/sseParser.js` 便于单测。
3. 新增 `tests/unit/aiSseClient.spec.js`：覆盖半包/粘连/未知事件。

## 验收标准

- vitest spec 覆盖半包/粘连/未知事件全部绿。
- 产出事件对象符合协议。

## 工作区域

- 新增 `src/services/aiSseClient.js`（+可选 `src/utils/sseParser.js`）、`tests/unit/aiSseClient.spec.js`

## 约束

- 不改 `TeachingAssistant.vue`（FE-09-1 会删它）。
- 纯 JS，无 TS。
