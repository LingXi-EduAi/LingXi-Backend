# BE-13-4 — 模型调用日志导出（CSV/JSON）

- **板块**：A4 模型调用日志（Part A 初赛）
- **波次**：A1
- **优先级**：MUST（大创材料 + MAT-03）
- **前置依赖**：BE-13-3
- **工作量**：0.5 人天

## 目标

一键导出模型调用日志作为大创交付物。

## 输入

- BE-13-3 的 service

## 实施内容

1. 扩展 `AiModelCallLogController`：`GET /api/ai/model-calls/export` 流式输出 CSV/JSON。
2. 表头：time,user,task,app,model,tokens,latency,cost,error。
3. 编写测试。

## 验收标准

- 导出文件格式正确、表头齐全。
- 测试绿。

## 工作区域

- 扩展 `AiModelCallLogController` + service；测试

## 约束

- 导出内容脱敏（配合 BE-14，Part B）。
- Java 8。
