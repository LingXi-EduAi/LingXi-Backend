# BE-14-2 — Redis 按用户限流 `/api/ai/**`

- **板块**：B2 隐私与安全加固（Part B 决赛，大创隐私）
- **波次**：B1
- **优先级**：Part B
- **前置依赖**：QA-00-1
- **工作量**：1 人天

## 目标

限制公网服务器滥用。

## 输入

- Redis 既有配置
- `config/WebMvcTokenConfig.java`（拦截器注册处）

## 实施内容

1. 新增 `config/RateLimitInterceptor.java` + `config/RateLimitConfig.java` + `service/RateLimitService.java`：对 `/api/ai/task*` 按用户返回 429 + `Retry-After`。
2. Lua 脚本原子实现。
3. 修改 `WebMvcTokenConfig` 注册。
4. 配置走 env。
5. 测试。

## 验收标准

- 超限返回 429 + `Retry-After`。
- Lua 原子。
- 配置走 env。
- 测试绿。

## 工作区域

- 新增 `config/RateLimitInterceptor.java` + `config/RateLimitConfig.java` + `service/RateLimitService.java`，修改 `WebMvcTokenConfig` 注册，测试

## 约束

- 凭据走 env。
- Java 8。
