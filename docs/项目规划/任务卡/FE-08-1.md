# FE-08-1 — 证据抽屉 `EvidenceDrawer.vue` + `safeMarkdown.js`

- **板块**：B5 子任务与证据面板（Part B 决赛）
- **波次**：B1
- **优先级**：Part B
- **前置依赖**：A5
- **工作量**：1.0 人天

## 目标

答案引用可视化（来源/片段/置信度/局限/安全外链）。

## 输入

- `retrieval_finished` 事件 + 答案引用（A3 落库形态）

## 实施内容

1. 新增 `src/components/ai/EvidenceDrawer.vue`：渲染来源/片段/置信度/局限/安全外链。
2. 新增 `src/utils/safeMarkdown.js`：**禁止 `v-html` 渲染未清洗内容**，安全链接处理（`rel="noopener noreferrer"`、协议白名单）。
3. `FeedbackPanel.vue` 预研（B12 细化）。
4. 集成进工作台。

## 验收标准

- 证据抽屉可交互。
- `safeMarkdown` 通过 XSS 用例测试。
- 测试绿。

## 工作区域

- 新增 `src/components/ai/EvidenceDrawer.vue`、`src/utils/safeMarkdown.js` + 集成

## 约束

- 禁止 `v-html` 渲染未清洗内容。
- 纯 JS。
