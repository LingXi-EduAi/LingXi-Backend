# FE-07-1 — 子任务/Agent 面板 `SubtaskPanel.vue`

- **板块**：B5 子任务与证据面板（Part B 决赛）
- **波次**：B1
- **优先级**：Part B
- **前置依赖**：A5
- **工作量**：1.5 人天

## 目标

工作台展示子任务树（Agent 类型/依赖/状态/重试入口）。

## 输入

- `task_decomposed`/`agent_assigned` 事件 + 任务快照（A3 落库形态）

## 实施内容

1. 新增 `src/components/ai/SubtaskPanel.vue`：渲染子任务树，失败子任务重试调 `/subtasks/{id}/retry`。
2. 集成进工作台。

## 验收标准

- 子任务树可交互（状态、重试）。
- 测试绿。

## 工作区域

- 新增 `src/components/ai/SubtaskPanel.vue` + 集成

## 约束

- 纯 JS。
