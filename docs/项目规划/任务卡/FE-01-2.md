# FE-01-2 — 弃用的 `request.post` 迁移到 `baseRequest.post`

- **板块**：A1 安全基线（Part A 初赛）
- **波次**：A0
- **优先级**：MUST
- **前置依赖**：无（独立清理）
- **工作量**：0.5 人天

## 目标

消灭弃用 `request.js` 的 6 处调用点，统一走 `baseRequest`。

## 输入

- `LingXi-Frontend/src/views/teacher/ClassManagement.vue`
- `LingXi-Frontend/src/views/teacher/ClassGroupingManagement.vue`
- `@/utils/api`（`baseRequest`）、`@/utils/request`（弃用）

## 实施内容

1. 将上述两个 `.vue` 文件中 `from "@/utils/request"` 的调用迁移为 `baseRequest`（`@/utils/api`）。
2. 保持调用参数语义一致。

## 验收标准

- grep 显示 `from "@/utils/request"` 仅剩 `api.js` 与 `TeachingAssistant.vue` 两处（后者由 FE-09-1 删除后清零）。

## 工作区域

- 仅 `ClassManagement.vue` 与 `ClassGroupingManagement.vue`。

## 约束

- 不引入新依赖。
- 不改 `baseRequest` 行为。
