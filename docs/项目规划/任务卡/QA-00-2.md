# QA-00-2 — 修复 9 个既有测试文件中跑失败的部分

- **板块**：A0 测试基建（Part A 初赛）
- **波次**：A0
- **优先级**：MUST
- **前置依赖**：QA-00-1
- **工作量**：0.5 人天

## 目标

基线测试套件全绿，后续任务才有稳定闸门。

## 输入

- `LingXi-Backend/src/test/java/**` 下 9 个测试类：AiTaskStatusTest、LingXiEventContractTest、DifyEventAdapterTest、OrderedEventBufferTest、AiEventServiceImplTest、AiSseSessionTest、AiTaskControlServiceImplTest、AuthorizationInterceptorTest、LxApplicationTests。

## 实施内容

1. 逐个运行测试，修复跑失败的部分（测试代码本身的 bug 优先修测试；若证明是生产代码 bug，修复测试点名的单个生产文件）。
2. `LxApplicationTests`（Spring 上下文加载）若因无 DB/Redis 失败，按"跳过或 profile 隔离"处理，并在测试注释中记录策略。

## 验收标准

- `mvn test` 全绿覆盖上述 9 个测试类。

## 工作区域

- 仅 `LingXi-Backend/src/test/java/**`（+ 若证明有 bug 的单个生产文件）

## 约束

- 不删除失败的测试来"通过"。
- Java 8。
