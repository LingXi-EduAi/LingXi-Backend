# 后端部署方案

## 一、项目概述

| 项目 | 说明 |
|------|------|
| 后端 | `LingXi-Backend`（Spring Boot 2.6.13 + MyBatis + MySQL 8 + Redis） |
| 前端 | `LingXi-Frontend`（Vue） |
| Java 版本 | 1.8 |
| 端口 | 后端 5678 |

## 二、环境要求

- **JDK** 1.8
- **Maven** 3.6+（或使用 IDE 内置 Maven）
- **MySQL** 8.0+
- **Redis** 5.0+
- **Node.js** 16+（前端构建）
- **IDE**：VSCode（推荐）或 IntelliJ IDEA

## 三、敏感配置管理方案

本项目采用 **环境变量 + `.env` 文件** 管理敏感配置（数据库密码、API Key 等），**不提交到 Git**。

### 文件说明

| 文件 | 是否提交 Git | 作用 |
|------|:---:|------|
| `application.properties` | ✅ | 敏感项用 `${ENV_VAR}` 占位符，非敏感项直接写值 |
| `.env.example` | ✅ | 环境变量模板，供参考 |
| `.env` | ❌ | 本地真实配置（群里私发） |

### 需要配置的环境变量

| 变量名 | 说明 | 示例 |
|--------|------|------|
| `DB_URL` | 数据库连接地址 | `jdbc:mysql://120.26.144.127:3306/lx?...` |
| `DB_USERNAME` | 数据库用户名 | `root` |
| `DB_PASSWORD` | 数据库密码 | `your-password` |
| `ALIYUN_SMS_ACCESS_KEY_ID` | 阿里云短信 AccessKey | `LTAI...` |
| `ALIYUN_SMS_ACCESS_KEY_SECRET` | 阿里云短信 Secret | `xxxx` |
| `API_KEY` | 旧版 Dify 聊天应用密钥 | `app-xxxx` |
| `DIFY_CHATFLOW_BASE_URL` | Dify Chatflow API 地址 | `http://120.26.144.127:6004/v1` |
| `DIFY_CHATFLOW_API_KEY` | Dify Chatflow 应用 API Key | `app-xxxx` |
| `DIFY_WORKFLOW_BASE_URL` | Dify Workflow API 地址 | `http://120.26.144.127:6004/v1` |
| `DIFY_WORKFLOW_API_KEY` | Dify 工作流应用 API Key | `app-xxxx` |
| `DIFY_CONNECT_TIMEOUT_MS` | Dify 建连超时，单位毫秒（可选） | `5000` |
| `DIFY_READ_TIMEOUT_MS` | Dify 工作流执行超时，单位毫秒（可选） | `120000` |

## 四、后端部署步骤

### 配置环境变量

```bash
# 复制模板文件
cp .env.example .env
```

编辑 `.env`，填入真实的配置值（数据库 IP、密码、API Key 等）：

```properties
DB_URL=jdbc:mysql://<云数据库IP>:3306/lx?allowPublicKeyRetrieval=true&useSSL=false&serverTimezone=Asia/Shanghai&useUnicode=true&characterEncoding=utf-8&allowMultiQueries=true&autoReconnect=true&autoReconnectForPools=true
DB_USERNAME=root
DB_PASSWORD=<真实数据库密码>

ALIYUN_SMS_ACCESS_KEY_ID=<真实AccessKey>
ALIYUN_SMS_ACCESS_KEY_SECRET=<真实Secret>

API_KEY=<真实API Key>

DIFY_CHATFLOW_BASE_URL=http://120.26.144.127:6004/v1
DIFY_CHATFLOW_API_KEY=<真实Dify Chatflow API Key>
DIFY_WORKFLOW_BASE_URL=http://120.26.144.127:6004/v1
DIFY_WORKFLOW_API_KEY=<真实Dify Workflow API Key>
DIFY_CONNECT_TIMEOUT_MS=5000
DIFY_READ_TIMEOUT_MS=120000
```

Chatflow 和 Workflow 当前由同一台 Dify 服务器提供，但属于不同应用，必须分别使用各自应用“访问 API”页面生成的 Key。

项目启动时会从当前工作目录自动读取 `.env`。生产环境也可以直接注入同名系统环境变量，系统环境变量的优先级高于 `.env`。

### 步骤 3：初始化数据库

#### 3.1 创建数据库

连接云数据库后执行：

```sql
CREATE DATABASE IF NOT EXISTS lx DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_general_ci;
```

#### 3.2 导入表结构

依次执行项目根目录下的 SQL 文件：

```bash
# 1. 导入基础表（11张表：班级、用户、成绩、作业等）
mysql -h <云数据库IP> -u root -p lx < lx.sql

# 2. 导入作业模块表 + 样例数据（2张表）
mysql -h <云数据库IP> -u root -p lx < lx_add.sql
```

或使用 Navicat / DBeaver / 阿里云 DMS 控制台手动导入。

#### 3.3 验证表结构

导入后应有 **13 张表**：

```
lx_class, lx_class_grouping, lx_conversation, lx_customer, lx_document,
lx_grade, lx_homework, lx_token, lx_study_group, lx_study_group_member,
lx_study_group_message, lx_homework_assignment, lx_homework_submission
```

### 步骤 4：启动 Redis

Windows 开发环境推荐使用 WSL Ubuntu：

```bash
sudo apt update
sudo apt install redis-server -y
sudo service redis-server start
redis-cli ping
```

返回 `PONG` 后，在 Windows PowerShell 中确认端口：

```powershell
Test-NetConnection 127.0.0.1 -Port 6379
```

项目本地配置使用 `localhost:6379`、数据库 `0`、无密码。生产环境应设置 Redis 密码并限制网络访问。

### 步骤 5：启动后端

#### 方式一：VSCode（推荐）

1. 用 VSCode 打开 `LingXi-Backend` 文件夹
2. 确保安装了 **Extension Pack for Java** 扩展
3. 确保运行配置的工作目录是 `LingXi-Backend` 项目根目录
4. 打开 `src/main/java/com/lxe/lx/LxApplication.java`，点击 `main` 方法上方的 **Run** 链接运行
5. 应用启动后访问 `http://localhost:5678`

项目会从工作目录自动读取 `.env`。如果 IDE 将工作目录设置到了其他位置，可以先在终端手动加载：

```powershell
Get-Content .env | ForEach-Object {
    if ($_ -match '^\s*([^#][^=]+)=(.*)$') {
        Set-Item -Path "Env:$($matches[1].Trim())" -Value $matches[2].Trim()
    }
}
```

#### 方式 1.2：VSCode + launch.json（自动加载 .env，推荐）

在项目根目录创建 `.vscode/launch.json`（此文件不会提交，已被 `.gitignore` 忽略）：

```json
{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "java",
            "name": "启动灵犀后端 (加载.env)",
            "request": "launch",
            "mainClass": "com.lxe.lx.LxApplication",
            "projectName": "LX",
            "envFile": "${workspaceFolder}/.env",
            "console": "integratedTerminal"
        }
    ]
}
```

创建后按 `F5` 即可启动，VSCode 会自动读取 `.env` 文件并注入环境变量，无需手动加载。

#### 方式二：命令行（PowerShell）

```powershell
# 当前 pom.xml 跳过 Spring Boot Maven run，使用编译类直接启动
mvn -q dependency:build-classpath "-Dmdep.outputFile=target/runtime-classpath.txt"
$classpath = "target\classes;" + (Get-Content target\runtime-classpath.txt -Raw).Trim()
java -cp $classpath com.lxe.lx.LxApplication
```

#### 方式三：命令行（Linux/Mac）

```bash
export $(grep -v '^#' .env | xargs)
mvn -q dependency:build-classpath -Dmdep.outputFile=target/runtime-classpath.txt
java -cp "target/classes:$(cat target/runtime-classpath.txt)" com.lxe.lx.LxApplication
```

#### 方式四：IntelliJ IDEA

1. 打开项目，等待 Maven 导入完成
2. 编辑运行配置（Run → Edit Configurations）
3. 在 `LxApplication` 的运行配置中，添加 Environment Variables（逐行填入 `.env` 中的变量）
4. 点击运行按钮

### 步骤 6：验证启动成功

控制台看到以下日志即表示启动成功：

```
Tomcat started on port(s): 5678 (http)
Started LxApplication in X.XXX seconds
```

### 步骤 7：调用 Dify

#### Chatflow

```http
POST http://localhost:5678/api/chatflow/messages
Content-Type: application/json
token: <灵犀登录Token>
```

```json
{
  "query": "你好",
  "inputs": {},
  "conversationId": ""
}
```

Chatflow 的结果位于统一响应的 `data.answer`，`data.conversation_id` 可用于下一轮对话。

查询 Chatflow 历史消息：

```http
GET http://localhost:5678/api/chatflow/messages?conversationId=<会话ID>&limit=100
token: <灵犀登录Token>
```

前端统一通过 `LingXi-Frontend/src/utils/dify.js` 调用后端，不保存或发送 Dify API Key。学生助教页面已经接入该中转接口。

#### Workflow

后端提供以下中转接口：

```http
POST http://localhost:5678/api/workflow/run
Content-Type: application/json
token: <灵犀登录Token>
```

当前 Workflow 开始节点包含：

| 变量 | 类型 | 是否必填 | 说明 |
|------|------|:---:|------|
| `doc` | `file` / `document` | 是 | 待处理文档，支持本地上传或远程 URL |
| `plan` | `paragraph` | 否 | 教学设计文本，最大长度 4090 |

本地文件需先调用 Dify `/files/upload` 获得 `upload_file_id`。后端运行 Workflow 时的请求结构如下：

```json
{
  "inputs": {
    "doc": {
      "transfer_method": "local_file",
      "upload_file_id": "<Dify文件ID>",
      "type": "document"
    },
    "plan": "根据文档生成教学设计"
  }
}
```

后端固定使用 Dify `blocking` 模式，并将 Dify 的完整 JSON 响应放在统一响应的 `data` 字段中：

```json
{
  "status": 200,
  "msg": "success",
  "data": {
    "workflow_run_id": "...",
    "data": {
      "status": "succeeded",
      "outputs": {}
    }
  }
}
```

推荐使用项目测试脚本完成上传和中转验证：

```powershell
.\test-dify-workflow.ps1 `
    -DocPath "D:\资料\课程文档.docx" `
    -Plan "根据文档生成教学设计"
```

只测试后端中转：

```powershell
.\test-dify-workflow.ps1 `
    -Mode Backend `
    -DocPath ".\README.md" `
    -Plan "根据文档生成教学设计"
```

Chatflow 测试脚本为 `test-dify-chatflow.ps1`，完整参数说明见 `dify连接测试.md`。
