# 后端更新总结

**更新日期：2026-07-18-00-13**

## 一、本次更新范围

本次完成了 Dify Chatflow、Workflow、Redis 登录校验以及前端智能助教之间的完整中转链路：

```text
灵犀前端
  -> 灵犀后端登录校验
  -> Dify Chatflow / Workflow
  -> 灵犀后端统一响应
  -> 灵犀前端展示结果
```

前端不再保存或直接发送 Dify API Key。

## 二、Dify 环境配置

Chatflow 和 Workflow 是两个独立的 Dify 应用，使用独立 API Key：

```properties
DIFY_CHATFLOW_BASE_URL=<chatflow-base-url>/v1
DIFY_CHATFLOW_API_KEY=<Chatflow API Key>

DIFY_WORKFLOW_BASE_URL=<workflow-base-url>/v1
DIFY_WORKFLOW_API_KEY=<Workflow API Key>

DIFY_CONNECT_TIMEOUT_MS=5000
DIFY_READ_TIMEOUT_MS=120000
```

Spring Boot 会读取后端根目录的 `.env`。修改 `.env` 后必须重启后端，已启动进程不会动态重新加载配置。

## 三、Chatflow 中转

### 发送消息

```http
POST /api/chatflow/messages
token: <灵犀登录Token>
Content-Type: application/json
```

```json
{
  "query": "1+1=？",
  "inputs": {},
  "conversationId": ""
}
```

返回结果中的 `data.answer` 是 Dify 回答，`data.conversation_id` 用于继续同一会话。

### 查询历史消息

```http
GET /api/chatflow/messages?conversationId=<会话ID>&limit=100
token: <灵犀登录Token>
```

后端使用登录用户 ID 作为 Dify `user`，前端不能伪造其他用户身份。

## 四、Workflow 中转

```http
POST /api/workflow/run
token: <灵犀登录Token>
Content-Type: application/json
```

当前 Workflow 开始节点定义：

| 变量 | 类型 | 必填 | 说明 |
|------|------|:---:|------|
| `doc` | 文档文件 | 是 | 支持本地上传和远程 URL |
| `plan` | 段落文本 | 否 | 教学设计文本，最大长度 4090 |

本地文件先通过 Dify `/files/upload` 上传，再将文件 ID 传入 Workflow：

```json
{
  "inputs": {
    "doc": {
      "transfer_method": "local_file",
      "upload_file_id": "<Dify文件ID>",
      "type": "document"
    },
    "plan": "根据文档生成教学设计"
  }
}
```

后端固定使用 `blocking` 模式，将 Dify 完整响应放在统一响应的 `data` 中。

## 五、测试脚本

新增脚本：

| 文件 | 作用 |
|------|------|
| `test-dify-chatflow.ps1` | 测试 Chatflow 直连和后端中转 |
| `test-dify-workflow.ps1` | 上传文档并测试 Workflow 直连和后端中转 |

Workflow 测试示例：

```powershell
.\test-dify-workflow.ps1 `
    -DocPath ".\README.md" `
    -Plan "根据文档生成教学设计"
```

脚本未传入真实登录 Token 时，会创建一个有效期 5 分钟的 Redis 临时 Token，并在测试结束后删除。完整说明见 `dify连接测试.md`。

## 六、前端接入

学生端 `TeachingAssistant.vue` 已完成改造：

- 移除 `VUE_APP_AI_KEY` 和浏览器直连 Dify 的代码。
- 通过 `src/utils/dify.js` 调用灵犀后端。
- 支持连续对话、会话 ID、历史消息加载、发送状态和错误显示。
- 当前使用 `blocking` 模式，Dify 执行完成后一次性显示回答。

## 七、Redis

本地 Redis 运行在 WSL Ubuntu：

```bash
sudo service redis-server start
redis-cli ping
```

返回 `PONG` 表示正常。项目使用 `localhost:6379`、数据库 `0`、本地无密码配置。

## 八、安全调整

- Dify API Key 仅保存在后端 `.env`。
- `.env` 已被 Git 忽略。
- 移除了原有 AI Service 中硬编码的 Dify Key。
- 旧 AI 接口和阿里云短信服务改为通过 Spring 配置读取密钥。
- 前端 `.env.example` 不再包含 Dify API Key。

旧 Key 曾经出现在源码中，建议在 Dify 控制台轮换。

## 九、验证结果

### 自动化验证

```text
后端测试：5 个通过，0 个失败
前端生产构建：成功
```

### Chatflow 真实中转

```text
发送状态：200
Dify 回答：正常返回
会话 ID：正常返回
历史查询状态：200
历史记录：成功读回同一条回答
```

### Workflow 真实中转

```text
文档上传：成功
Workflow 直连状态：succeeded
后端中转状态：200
后端 Workflow 状态：succeeded
outputs.result：正常返回
Redis 临时 Token：已删除
```

## 十、主要文件

- `src/main/java/com/lxe/lx/controller/DifyChatflowController.java`
- `src/main/java/com/lxe/lx/controller/DifyWorkflowController.java`
- `src/main/java/com/lxe/lx/service/impl/DifyChatflowServiceImpl.java`
- `src/main/java/com/lxe/lx/service/impl/DifyWorkflowServiceImpl.java`
- `test-dify-chatflow.ps1`
- `test-dify-workflow.ps1`
- `dify连接测试.md`
- `.env.example`
- `后端部署方案.md`

## 十一、云数据库初始化与注册验证（2026-07-18）

### 问题根因

前端注册时返回“账号注册失败”，后端日志中的实际错误为：

```text
Unknown database 'lx'
```

云 MySQL 服务器和账号均可正常连接，但 `.env` 中 `DB_URL` 指向的 `lx` 数据库尚未创建，导致注册接口在查询 `lx_customer` 时失败。

### 数据库初始化

- 在 `.env` 当前指向的云 MySQL 上创建 `lx` 数据库，字符集为 `utf8mb4`。
- 导入 `lx.sql` 中的 11 张基础业务表。
- 从 `lx_add.sql` 导入 `lx_homework_assignment` 和 `lx_homework_submission` 两张表的结构。
- 未导入 `lx_add.sql` 中的作业演示数据。
- 初始化后共 13 张业务表，注册依赖的 `lx_customer` 已正常创建。

### 实际验证

使用临时账号完成了真实接口验证：

```text
POST /customer/add：status 200，注册成功
lx_customer：成功写入 1 条临时用户记录
POST /token/login：status 200，登录成功
Redis Token：成功返回 32 位 Token
```

验证结束后已删除临时用户、数据库 Token 记录和 Redis Token，未在云数据库中保留测试账号。

### 当前运行状态

```text
前端：http://localhost:8080
后端：http://localhost:5678
Redis：localhost:6379
云数据库：lx，13 张业务表
```

注册、登录、MySQL 持久化和 Redis Token 链路均已验证正常。
